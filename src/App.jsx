import React from "react";
import "./App.css";

const x = +1
const y = +2

const initialState = {
  clicks: { amount: 0 },
  auto: { cost: 10, amount: 0 },
};

function App () {
    return (
      <main>
        <p>React State Lab</p>
      </main>
    );
}

export default App;
